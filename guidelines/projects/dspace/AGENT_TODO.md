# AGENT_TODO — dspace

## Resuming a session

1. Run git orientation in the mounted repo:
   - `git branch --show-current | cat`
   - `git --no-pager status | cat`
   - `git --no-pager log --oneline -5 | cat`
2. Read `.agents/AGENTS.md` and `.agents/AGENT_PROMPT.md`.
3. Read top entry of `.agents/AGENT_DONE.md`.
4. Continue from the first unchecked subtask below.

---

## Initial DSpace onboarding setup

- [ ] Confirm target branch and ticket key with developer before first code changes
- [ ] Capture preferred local build/test command set in `.agents/AGENTS.md` if missing
- [ ] Verify with the developer that the task is complete

