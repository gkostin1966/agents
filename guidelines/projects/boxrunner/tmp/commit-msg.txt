style: enforce frozen string literal comments across Ruby sources

- enable Style/FrozenStringLiteralComment with always_true in .rubocop.yml
- auto-correct Ruby files to add # frozen_string_literal: true where missing
- align app, config, tasks, specs, and tests with the enforced style rule
