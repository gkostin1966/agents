# New Session Startup Prompt — dspace-angular

Project-specific startup overrides for `mlibrary/dspace-angular`.

## Task Files

Read these files in order:

1. `AGENTS_ROOT/guidelines/projects/dspace-angular/AGENT_TODO.md`
2. `AGENTS_ROOT/guidelines/projects/dspace-angular/AGENT_DONE.md` (top entry)
3. `AGENTS_ROOT/guidelines/projects/dspace-angular/PLAN_DSPACE_ANGULAR_PASSWORD_LOGIN.md` (only when working DEEPBLUE-466)

## Quiz Gate

Ask: **"Should I take the onboarding quiz in `AGENT_QUIZ.md`?"**

- Yes: answer all questions before implementation, then stop and report completion.
- No/Skip: continue directly to tasks.

Do not read `AGENT_QUIZ_ANSWERS.md` until all answers are written and the developer explicitly permits comparison.

## Startup Workflow

- Map branch name to ticket key (`DEEPBLUE-<id>`) where possible.
- State whether the current work is app-code, `.agents` bookkeeping, or both.
- If `.agents` task files are stale but fixable without ambiguity, correct them before coding.

